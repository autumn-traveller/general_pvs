# The Peer View Sampling Protocol

This repository serves as a library implementing the PVS specification.

It also includes a wireshark dissector. To install it copy or symlink the dissector into one of the directories listed in your wireshark settings ("Help" -> "About wireshark" -> "Folders" , or "Plugins")

## Building

run `cargo build`
